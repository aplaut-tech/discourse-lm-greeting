{{#if canCreateTopic}}
  {{d-button
    id="create-topic"
    class="btn btn-default"
    action=action
    icon="plus"
    disabled=disabled
    label="topic.create"}}
{{else}}
  {{d-button
    id="create-topic"
    class="btn btn-default"
    action=action
    icon="plus"
    label="topic.create"}}
{{/if}}
